import Shell from "@/components/Shell";import {db} from "@/lib/db";import {money,calc} from "@/lib/money";import {notFound} from "next/navigation";import {paid} from "./actions";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const x=await db.consignment.findUnique({where:{id},include:{customer:true}});if(!x)return notFound();const c=calc(x.salePriceCents,x.customerPercentBps,x.feeCents);const url=`${process.env.NEXT_PUBLIC_APP_URL||""}/sign/${x.acceptanceToken}`;return <Shell><div className="top"><div><h1>{x.reference}</h1><div className="muted">{x.title}</div></div><span className="badge">{x.status}</span></div><div className="form"><div className="card"><h2>{x.customer.name}</h2><p>{x.customer.email}<br/>{x.customer.phone}</p><p>Platform: <b>{x.platform||"—"}</b><br/>Method: <b>{x.method}</b></p><h3>Digital acceptance</h3><input style={{width:"100%",padding:10}} readOnly value={url}/><p className="muted">Send this link by text or email.</p></div><div className="card"><h2>Payout Breakdown</h2><div className="summary"><div className="row"><span>Sale</span><b>{money(x.salePriceCents)}</b></div><div className="row"><span>Customer ({x.customerPercentBps/100}%)</span><b>{money(c.customer)}</b></div><div className="row"><span>ITNX gross</span><b>{money(c.gross)}</b></div><div className="row"><span>Fee</span><b>-{money(x.feeCents)}</b></div><div className="row big"><span>ITNX net</span><span>{money(c.net)}</span></div></div><h3>Pay customer: {money(c.customer)}</h3>{!x.paid?<form action={paid.bind(null,x.id)}><div className="field"><label>Check / ACH / Cash reference</label><input name="ref"/></div><br/><button className="button">Mark Paid</button></form>:<p><b>✓ PAID</b> {x.payoutReference}</p>}</div></div></Shell>}