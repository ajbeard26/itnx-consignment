import Shell from "@/components/Shell";import {db} from "@/lib/db";import {save} from "./actions";export default async function Page(){const s=await db.settings.upsert({where:{id:1},update:{},create:{id:1}});return <Shell><h1>Settings</h1><div className="card" style={{maxWidth:600}}><form action={save}><div className="field"><label>Default customer percentage</label><input name="percent" type="number" min="0" max="100" step=".01" defaultValue={s.defaultCustomerPercentBps/100}/><small className="muted">Default is 50%. This only affects new consignments and can be overridden on each deal.</small></div><br/><button className="button">Save</button></form></div></Shell>}