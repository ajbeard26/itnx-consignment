"use server";import {db} from "@/lib/db";import {redirect} from "next/navigation";export async function accept(token:string,fd:FormData){const x=await db.consignment.findUnique({where:{acceptanceToken:token}});if(!x)throw new Error("Invalid");await db.consignment.update({where:{id:x.id},data:{acceptedName:String(fd.get("name")),acceptedAt:new Date(),status:"ACCEPTED"}});redirect(`/sign/${token}?signed=1`)}