import {db} from "@/lib/db";import {calc,money} from "@/lib/money";import {notFound} from "next/navigation";import {accept} from "./actions";export default async function Page({params,searchParams}:{params:Promise<{token:string}>,searchParams:Promise<{signed?:string}>}){const{token}=await params;const q=await searchParams;const x=await db.consignment.findUnique({where:{acceptanceToken:token},include:{customer:true}});if(!x)return notFound();const c=calc(x.salePriceCents,x.customerPercentBps,x.feeCents);return <div className="customer"><div style={{textAlign:"center"}}><h1>ITNX Consignment</h1><div className="muted">A service of NXRENT LLC</div></div><br/><div className="card"><h2>Payout Authorization</h2>{q.signed||x.acceptedAt?<><h2>✓ Accepted</h2><p>Your acceptance has been recorded.</p><p className="big">{money(c.customer)} via {x.method}</p></>:<><p>Hello <b>{x.customer.name}</b>. Please review your consignment payout.</p><div className="summary"><div className="row"><span>Item</span><b>{x.title}</b></div><div className="row"><span>Sale price</span><b>{money(x.salePriceCents)}</b></div><div className="row"><span>Your share</span><b>{x.customerPercentBps/100}%</b></div><div className="row big"><span>Your payout</span><span>{money(c.customer)}</span></div><div className="row"><span>Method</span><b>{x.method}</b></div></div><p>By signing, I acknowledge the sale information and agree that my payout is <b>{money(c.customer)}</b>.</p><form action={accept.bind(null,token)}><div className="field"><label>Type your full legal name as your signature</label><input name="name" required/></div><br/><label><input type="checkbox" required/> I agree to the payout above.</label><br/><br/><button className="button">Accept & Sign</button></form></>}</div></div>}